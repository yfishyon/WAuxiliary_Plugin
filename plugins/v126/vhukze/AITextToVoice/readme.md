# AI文字转语音

## 用法

1. 插件有问题 TG群找vhukze
2. 我这里用的硅基流动的api，只需要替换token就可以了，如果你用的是其他api，看情况自己修改参数
3. 发送指定触发词开头的消息，指定内容后加/描述语，用来给ai提示。例如发送：语音：早上好啊，你在干什么/语气自然一点，用粤语说
4. 触发词默认为 “语音：” 或 “语音:”，提示语可带可不带
5. 可以自己修改触发词，优先匹配第一个 （最多两个，想匹配多个自行改代码）
